# =============================================================================
# Xibo PWA Player — Configuration
# =============================================================================
# This file is sourced by /opt/xibo-player/launch-kiosk.sh
# Edit and restart the service:
#   systemctl --user restart xibo-player-kiosk.service
# =============================================================================

# ---------------------------------------------------------------------------
# CMS URL  (REQUIRED)
# ---------------------------------------------------------------------------
# The full URL to your Xibo CMS PWA player endpoint.
# Example: https://h1.superpantalles.com:8081/player/pwa/
CMS_URL=https://your-cms.example.com:8081/player/pwa/

# ---------------------------------------------------------------------------
# Browser
# ---------------------------------------------------------------------------
# Which browser to use for kiosk mode.
# Supported values: chromium, chrome, google-chrome-stable, firefox
BROWSER=chromium

# ---------------------------------------------------------------------------
# Display Key  (optional)
# ---------------------------------------------------------------------------
# Override the auto-generated hardware key. Leave empty for auto-detection.
# If set, this value is appended as ?displayKey=VALUE to the CMS URL.
DISPLAY_KEY=

# ---------------------------------------------------------------------------
# Extra Browser Flags  (optional)
# ---------------------------------------------------------------------------
# Additional command-line flags passed to the browser.
# Space-separated. Examples:
#   --disable-gpu                   Disable GPU acceleration
#   --window-size=1920,1080         Force resolution
#   --force-device-scale-factor=1   Fix scaling on HiDPI
#   --enable-logging=stderr         Browser debug logging
EXTRA_BROWSER_FLAGS=
